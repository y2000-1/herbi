# HerbiEstim Backend — Cross-platform dependencies
# Runs on Windows, Linux, macOS. No system-specific packages.
#
# NOTE: torch and torchvision should be installed separately
#       based on your platform and CUDA availability:
#   GPU (CUDA 12.x, e.g. RTX 4090):
#     pip install torch>=2.4.0 torchvision>=0.19.0 --index-url https://download.pytorch.org/whl/cu121
#   GPU (CUDA 11.x):
#     pip install torch>=2.4.0 torchvision>=0.19.0 --index-url https://download.pytorch.org/whl/cu118
#   CPU:
#     pip install torch>=2.4.0 torchvision>=0.19.0 --index-url https://download.pytorch.org/whl/cpu

# ── Image processing (headless — no GUI dependencies) ──
opencv-python-headless>=4.8.0
Pillow>=9.0.0
scikit-image>=0.19.0
imutils>=0.5.3
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# ── Data ──
pandas>=1.3.0

# ── Web framework ──
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
pydantic>=2.0.0

# ── pix2pix dependencies ──
dominate>=2.4.0
visdom>=0.2.4
wandb>=0.13.0

# ── SAM mode (optional, required for leaf segmentation) ──
segment-anything
supervision
transformers>=4.48.0,<4.50.0
