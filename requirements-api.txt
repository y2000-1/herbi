# HerbiEstim API — Minimal dependencies for inference only
# NOTE: torch and torchvision are installed separately in the Dockerfile
#       (CPU or GPU version selected via --build-arg USE_GPU).
#       If running locally without Docker, install them manually:
#         pip install torch>=2.0.0 torchvision>=0.15.0

# Image processing
opencv-python-headless>=4.5.0
Pillow>=9.0.0
scikit-image>=0.19.0
imutils>=0.5.3
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# Data
pandas>=1.3.0

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
pydantic>=2.0.0

# pix2pix dependencies
dominate>=2.4.0

# SAM mode
segment-anything
supervision
transformers>=4.48.0,<4.50.0
